#!/usr/bin/env bash

FILE="$HOWTO/$1"

# Check if a file argument is provided
if [ -z "$1" ]; then
  echo "Usage: howto <file>"
  exit 1
fi
  
# Create the file and its parents if it doesn't exist
if [ ! -f "$FILE" ]; then
    mkdir -pv $(dirname "$FILE")
fi
vim -N "$FILE"

